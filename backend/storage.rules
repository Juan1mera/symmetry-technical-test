rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Regla para la carpeta especifica que pide el README
    match /media/articles/{imageId} {
      // Cualquiera puede ver la imagen
      allow read: if true;
      // Solo usuarios logueados pueden subir, y debe ser una imagen
      allow write: if request.auth != null 
                   && request.resource.contentType.matches('image/.*');
    }
  }
}